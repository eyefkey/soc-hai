# Copy to .env.local for local development.
#
# Server-side only. The access token is attached to backend calls inside
# server components and server actions, so no API origin or credential is
# exposed to the browser — hence no NEXT_PUBLIC_ prefix.
API_URL=http://localhost:4000
